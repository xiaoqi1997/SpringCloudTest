server:
  port: 8006

spring:
  application:
    name: consul-provider-payment
  # 注册中心
  cloud:
    consul:
      host: 82.157.186.132
      port: 8500
      discovery:
        service-name: ${spring.application.name}